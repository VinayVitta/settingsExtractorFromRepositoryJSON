# Allowed frontend host(s) for CORS (comma-separated if multiple)
FRONTEND_ORIGINS=http://localhost:3000,https://myfrontend.com

# Optional: backend port
API_PORT=8000

# Environment (optional)
ENV=development
